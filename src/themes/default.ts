// Default theme

// ----------------------------------------------------------------------------
// IMPORTS

/* Local */
import { ITheme } from "./interface";

// ----------------------------------------------------------------------------

const theme: ITheme = {
  colors: {
    orange: "#FFA500",
  },
};

export default theme;
